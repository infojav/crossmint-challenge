export const __CONSTANTS__ = {
    // Purpose: To store constants used throughout the application.
    SERVER_PORT: 3000,
    CROSSMINT_CHALLENGE_API: "https://challenge.crossmint.io/api/",

    // 0 -> rows and cols >= 0,
    // 1 -> rows and cols >= 1
    UNIVERSE_START: 0,
    MAX_UNIVERSE_SIZE: 2e32 - 1,
};
